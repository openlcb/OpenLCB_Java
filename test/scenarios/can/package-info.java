/**

Directory of high-level tests and examples for OpenLCB CAN wire protocol.
<p>
Unit tests are kept in the same package as the code being tested.
This package contains tests that involve the interaction of
multiple classes to implement nodes, links, gateways, etc,
in the CAN wire protocol.

*/
package scenarios.can;
